Dynamic lists used by the Palo Alto firewalls to automatically distribute shared lists, such as IP addresses or domain names that are either allowed or denied depending on how the rule is configured.
